---
phase: 01-funda-o-backend-multi-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - .gitignore
  - .env.example
  - README.md
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/vitest.config.ts
  - apps/web/tests/skeleton.test.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
autonomous: false
requirements: [AUTH-04]
must_haves:
  truths:
    - "pnpm install na raiz instala o monorepo inteiro sem erro"
    - "pnpm --filter web build compila o app Next.js 16 sem erro"
    - "O teste de esqueleto existe e FALHA (vermelho) — prova que o caminho feliz ainda não existe"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo pnpm com apps/* e packages/*"
    - path: "apps/web/package.json"
      provides: "App Next.js 16.2 com dependências pinadas (typescript ~5.9.3, next 16.2.10, @supabase/ssr 0.12.0)"
    - path: "apps/web/tests/skeleton.test.ts"
      provides: "Teste ponta a ponta do caminho feliz do esqueleto (login gestor + leitura da própria organização)"
    - path: ".env.example"
      provides: "Contrato de variáveis de ambiente do projeto"
  key_links:
    - from: "apps/web/vitest.config.ts"
      to: "apps/web/tests/skeleton.test.ts"
      via: "vitest run descobre e executa o teste"
      pattern: "tests"
---

<objective>
Criar o monorepo pnpm do produto (apps/web com Next.js 16 App Router + packages/shared), com todas as dependências pinadas nas versões verificadas pela auditoria de legitimidade de pacotes, e o teste ponta a ponta do esqueleto escrito e falhando (vermelho).

Purpose: base de código sobre a qual todas as fases constroem (stack travada em .claude/CLAUDE.md). O teste vermelho define o alvo do walking skeleton: "gestor autentica e lê a própria organização".
Output: monorepo compilando, teste de esqueleto vermelho, .env.example documentando o contrato de ambiente.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-backend-multi-tenant/01-CONTEXT.md
@.planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

## Phase Goal

**Como** gestor de um escritório jurídico, **quero** entrar no painel web e gerenciar minha equipe de advogados, **para que** minha organização exista com dados isolados e criptografados em região brasileira desde o primeiro dia.

*(História derivada literalmente dos success criteria da Fase 1 no ROADMAP.md — o Goal da fase no roadmap não está em formato de user story.)*

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Aprovar auditoria de legitimidade dos pacotes npm</name>
  <action>Apresentar ao usuário a tabela de auditoria abaixo (12 pacotes, todos com verdict OK ou SUS por "too-new", nenhum SLOP) e AGUARDAR aprovação humana explícita antes de qualquer instalação. Este gate nunca é auto-aprovável (blocking-human). Após "aprovado", prosseguir para a Task 2 usando EXATAMENTE as versões listadas.</action>
  <done>Usuário respondeu "aprovado" (ou indicou substituições, que devem ser incorporadas antes da Task 2)</done>
  <what-built>A pesquisa da fase (01-RESEARCH.md, seção "Package Legitimacy Audit") auditou os 12 pacotes npm que serão instalados nesta fase. Nenhum pacote recebeu verdict [SLOP]. Todos os flags [SUS] têm razão única "too-new" (release recente — ciclo rápido dos projetos), com repositório oficial confirmado e dezenas de milhões de downloads semanais. A mitigação prescrita é pinar versões exatas verificadas em 2026-07-08.</what-built>
  <how-to-verify>
    Pacotes a aprovar (todos canônicos, versão exata pinada):
    1. next@16.2.10 — npmjs.com/package/next (repo: github.com/vercel/next.js, 38M downloads/semana)
    2. react@19.2.7 + react-dom@19.2.7 — npmjs.com/package/react (github.com/facebook/react, 141M/sem)
    3. typescript@~5.9.3 — npmjs.com/package/typescript (github.com/microsoft/TypeScript) — ATENÇÃO: latest é 7.0.2; o projeto trava 5.9.x
    4. @supabase/supabase-js@2.110.1 — npmjs.com/package/@supabase/supabase-js (github.com/supabase/supabase-js)
    5. @supabase/ssr@0.12.0 — npmjs.com/package/@supabase/ssr (github.com/supabase/ssr)
    6. tailwindcss@4.3.2 — npmjs.com/package/tailwindcss (github.com/tailwindlabs/tailwindcss)
    7. zod@4.4.3 — npmjs.com/package/zod (github.com/colinhacks/zod)
    8. @tanstack/react-query@5.101.2 — npmjs.com/package/@tanstack/react-query (github.com/TanStack/query)
    9. resend@6.17.2 — npmjs.com/package/resend (github.com/resend/resend-node, sem postinstall)
    10. vitest@4.1.10 — npmjs.com/package/vitest (github.com/vitest-dev/vitest)
    11. supabase@2.109.1 (CLI, devDep) — npmjs.com/package/supabase (github.com/supabase/cli)

    Se quiser conferir, abra qualquer link acima e confirme que o pacote aponta para o repositório oficial indicado. Nenhuma instalação acontece antes da sua aprovação.
  </how-to-verify>
  <resume-signal>Digite "aprovado" para liberar a instalação, ou descreva qualquer pacote que queira remover/substituir</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold do monorepo pnpm com versões pinadas e teste de esqueleto vermelho</name>
  <files>pnpm-workspace.yaml, package.json, .gitignore, .env.example, README.md, apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css, apps/web/vitest.config.ts, apps/web/tests/skeleton.test.ts, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md (seções "Standard Stack", "Recommended Project Structure", "Pitfall 2")
    - .claude/CLAUDE.md (tabela "Version Compatibility" e "What NOT to Use")
  </read_first>
  <action>
    Habilitar pnpm via corepack (Node 22 já traz corepack: rodar corepack enable pnpm; pnpm não está instalado globalmente nesta máquina).

    Criar a raiz do monorepo: pnpm-workspace.yaml com packages apps/* e packages/*; package.json raiz privado com devDependencies compartilhadas supabase@2.109.1 e typescript@~5.9.3, e scripts test (pnpm -r vitest run) e build (pnpm --filter web build); .gitignore cobrindo node_modules, .next, .env, .env.local, .vercel, supabase/.temp.

    Criar apps/web como app Next.js 16 App Router manualmente (não usar create-next-app latest — ele instalaria versões não auditadas): package.json com dependencies pinadas EXATAS conforme aprovado na Task 1 (next 16.2.10, react 19.2.7, react-dom 19.2.7, @supabase/supabase-js 2.110.1, @supabase/ssr 0.12.0, zod 4.4.3, @tanstack/react-query 5.101.2, resend 6.17.2, tailwindcss 4.3.2) e devDependencies typescript ~5.9.3, vitest 4.1.10, @types/react, @types/node, @vitejs/plugin-react (necessário para vitest com tsx). Pitfall 2 da pesquisa: NUNCA instalar typescript sem versão — latest hoje é 7.0.2 e o projeto trava 5.9.x. Tailwind 4 via @tailwindcss/postcss em postcss.config.mjs e @import "tailwindcss" em app/globals.css (zero-config v4). app/layout.tsx com lang="pt-BR" e metadata título "Copiloto Jurídico". app/page.tsx mínima redirecionando para /login (rota criada no plano 01-04; por ora um link simples serve).

    Criar packages/shared: package.json (name @copiloto/shared, main src/index.ts) e src/index.ts exportando os tipos de domínio compartilhados: type UserRole = 'super_admin' | 'gestor' | 'advogado'; type ProfileStatus = 'active' | 'removed'; type InvitationStatus = 'pending' | 'accepted' | 'cancelled' | 'expired'. Esses literais são o contrato dos enums do schema (plano 01-02) — mesmos nomes, mesma grafia.

    Criar apps/web/vitest.config.ts (environment node, include tests/**/*.test.ts, carregando .env.local via dotenv ou loadEnv do vite).

    Escrever o teste de esqueleto apps/web/tests/skeleton.test.ts (o teste ponta a ponta que DEVE FALHAR agora): usando createClient de @supabase/supabase-js com process.env.NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY, (1) signInWithPassword com SEED_GESTOR_EMAIL/SEED_USER_PASSWORD do env, (2) select em organizations esperando exatamente 1 linha com name igual a "Escritório Demo", (3) select do próprio profile esperando role "gestor". Se as variáveis de ambiente estiverem ausentes, o teste deve FALHAR com mensagem clara em pt-BR (não usar skip — o vermelho é o estado esperado até o plano 01-02 concluir).

    Criar .env.example documentando o contrato de ambiente com placeholders: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (server-only, nunca exposta ao cliente), SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, NEXT_PUBLIC_SITE_URL, SEED_GESTOR_EMAIL, SEED_ADVOGADO_EMAIL, SEED_SUPERADMIN_EMAIL, SEED_USER_PASSWORD.

    Criar README.md raiz em pt-BR com: visão de uma linha do produto, comando de execução full-stack local (corepack enable pnpm && pnpm install && pnpm --filter web dev — o app roda contra o projeto Supabase hospedado em sa-east-1; Docker não é necessário para desenvolver), e comandos de teste (pnpm --filter web vitest run; pgTAP roda no CI).

    Rodar pnpm install e pnpm --filter web build para provar que o scaffold compila.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter web build && pnpm --filter web exec vitest run tests/skeleton.test.ts; test $? -ne 0</automated>
  </verify>
  <done>Monorepo instala e compila; teste de esqueleto existe e falha (vermelho) por ausência do projeto Supabase</done>
  <acceptance_criteria>
    - `pnpm install` na raiz termina com exit 0
    - `pnpm --filter web build` termina com exit 0
    - `pnpm --filter web exec vitest run tests/skeleton.test.ts` termina com exit DIFERENTE de 0 (vermelho esperado)
    - `apps/web/package.json` contém `"next": "16.2.10"` e devDependencies contém `"typescript": "~5.9.3"`
    - `node -e "const v=require('./apps/web/node_modules/typescript/package.json').version; if(!v.startsWith('5.9')) process.exit(1)"` termina com exit 0
    - `packages/shared/src/index.ts` contém `super_admin`, `gestor`, `advogado`, `active`, `removed`, `pending`, `accepted`, `cancelled`, `expired`
    - `.env.example` contém `NEXT_PUBLIC_SUPABASE_URL=` e `SUPABASE_SERVICE_ROLE_KEY=`
    - `README.md` contém `pnpm --filter web dev`
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (este plano)

- Arquivos novos: `pnpm-workspace.yaml`, `package.json` (raiz), `.gitignore`, `.env.example`, `README.md`, `apps/web/` (scaffold Next.js 16: `package.json`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `vitest.config.ts`, `tests/skeleton.test.ts`), `packages/shared/` (`package.json`, `tsconfig.json`, `src/index.ts`)
- Símbolos novos: tipos `UserRole`, `ProfileStatus`, `InvitationStatus` (exports de `@copiloto/shared`)
- Contrato de ambiente: variáveis `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `NEXT_PUBLIC_SITE_URL`, `SEED_GESTOR_EMAIL`, `SEED_ADVOGADO_EMAIL`, `SEED_SUPERADMIN_EMAIL`, `SEED_USER_PASSWORD`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Código de terceiros entra no build e no servidor |
| Repositório → variáveis de ambiente | Secrets nunca podem entrar no git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | npm installs (12 pacotes) | mitigate | Auditoria de legitimidade (01-RESEARCH.md) + checkpoint humano bloqueante (Task 1) + versões exatas pinadas — nunca `latest` |
| T-1-01 | Information Disclosure | .env / .env.local no git | mitigate | `.gitignore` cobre `.env*`; apenas `.env.example` com placeholders é versionado |
| T-1-02 | Tampering | typescript@latest (7.0.2) quebra a toolchain validada | mitigate | Pin `~5.9.3` em todos os package.json + assert automatizado na acceptance criteria |
</threat_model>

<verification>
- `pnpm install` e `pnpm --filter web build` verdes na raiz do monorepo
- Teste de esqueleto vermelho (exit != 0) com mensagem clara
- Nenhum arquivo `.env` ou `.env.local` rastreado pelo git (`git status --porcelain` não lista .env)
</verification>

<success_criteria>
- Monorepo pnpm funcional com apps/web (Next 16.2.10) e packages/shared
- Todas as versões pinadas conforme auditoria aprovada
- Teste ponta a ponta do esqueleto escrito e vermelho — alvo definido para os planos 01-02 e 01-04
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-backend-multi-tenant/01-01-SUMMARY.md` when done
</output>
