---
phase: 01-funda-o-backend-multi-tenant
plan: 08
type: execute
wave: 6
depends_on: ["01-05", "01-06", "01-07"]
files_modified:
  - vercel.json
  - README.md
  - supabase/config.toml
autonomous: false
requirements: [AUTH-04, LGPD-01]
user_setup:
  - service: vercel
    why: "Hospedagem do painel + API (stack contract em .claude/CLAUDE.md); deploy do walking skeleton em ambiente real"
    env_vars:
      - name: VERCEL_TOKEN
        source: "vercel.com -> Account Settings -> Tokens (só se o deploy via CLI pedir autenticação; a integração GitHub->Vercel dispensa o token)"
    dashboard_config:
      - task: "Ter uma conta Vercel (hobby serve para o dev deploy do beta)"
        location: "vercel.com"
must_haves:
  truths:
    - "O painel está no ar em uma URL Vercel: gestor demo loga, vê a própria organização e a tela de equipe funciona"
    - "As funções do app rodam na região gru1 (São Paulo) e o banco permanece só no Supabase sa-east-1 (Assumption A6 — dados em repouso cobertos por LGPD-01)"
    - "O dono verificou pessoalmente o fluxo completo do esqueleto no ambiente deployado"
  artifacts:
    - path: "vercel.json"
      provides: "Config do deploy: região gru1, root do app no monorepo"
    - path: "README.md"
      provides: "Seção de deploy: URL do ambiente, variáveis necessárias, como fazer redeploy"
  key_links:
    - from: "Vercel (produção dev)"
      to: "projeto Supabase sa-east-1"
      via: "variáveis NEXT_PUBLIC_SUPABASE_URL / ANON_KEY / SUPABASE_SERVICE_ROLE_KEY nos secrets do projeto Vercel"
      pattern: "SUPABASE"
    - from: "supabase/config.toml"
      to: "URL do deploy"
      via: "site_url/additional_redirect_urls incluem a URL Vercel (reset de senha e convites funcionam no ar)"
      pattern: "additional_redirect_urls"
---

<objective>
Colocar o walking skeleton no ar: deploy do apps/web na Vercel (região gru1), variáveis de ambiente configuradas, redirect URLs do Supabase atualizadas para a URL pública, e verificação final humana do fluxo completo da fase no ambiente real.

Purpose: fecha o requisito de deployment do walking skeleton e dá ao dono (não-técnico) uma URL real para validar a fase. A Fase 2 (extensão Chrome) precisa de uma API pública — nasce aqui.
Output: URL Vercel funcional + aprovação humana do esqueleto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md
@.env.example
@supabase/config.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy na Vercel com região gru1 e redirects atualizados</name>
  <files>vercel.json, README.md, supabase/config.toml</files>
  <read_first>
    - .planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md (Assumption A6 — gru1; Environment Availability — Vercel CLI ausente, usar pnpm dlx vercel ou integração GitHub)
    - .env.example (lista completa de variáveis a configurar na Vercel)
    - supabase/config.toml (site_url/redirects atuais a estender)
  </read_first>
  <action>
    Criar vercel.json na raiz com regions ["gru1"] (A6: latência; dados em repouso seguem no Supabase sa-east-1). Deploy do monorepo: usar `pnpm dlx vercel` (link do projeto com root directory apps/web, framework Next.js detectado) — se pedir login e não houver VERCEL_TOKEN, abrir gate de autenticação para o usuário; alternativa equivalente: conectar o repositório GitHub pela integração Vercel.

    Configurar as variáveis de ambiente do projeto Vercel (via `pnpm dlx vercel env add` ou dashboard): NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (server-only), NEXT_PUBLIC_SITE_URL (a URL do deploy), EMAIL_DRIVER (console até o Resend estar verificado), EMAIL_FROM_NAME, EMAIL_FROM_ADDRESS.

    Atualizar supabase/config.toml: additional_redirect_urls passa a incluir https://<url-do-deploy>/auth/confirm (reset de senha no ar) e site_url de produção; aplicar com `pnpm dlx supabase config push` (fallback: registrar no SUMMARY os ajustes manuais em Auth -> URL Configuration, incluindo qualquer pendência herdada do 01-02).

    Fazer o deploy de produção (`pnpm dlx vercel --prod` ou merge na branch conectada) e smoke test automatizado: curl na URL raiz retorna 200/3xx e o HTML do /login contém o título do produto; registrar a URL no README.md (seção "Deploy" com variáveis e comando de redeploy) e no SUMMARY.
  </action>
  <verify>
    <automated>URL=$(grep -oE "https://[a-z0-9.-]+\.vercel\.app" README.md | head -1) && test -n "$URL" && curl -s -o /dev/null -w "%{http_code}" -L "$URL/login" | grep -qE "^(200)$" && grep -q "gru1" vercel.json</automated>
  </verify>
  <done>App no ar na Vercel (gru1), variáveis configuradas, /login respondendo 200, redirects do Supabase atualizados</done>
  <acceptance_criteria>
    - `vercel.json` contém `gru1`
    - `curl -L <url>/login` retorna HTTP 200 com HTML contendo `Copiloto`
    - `README.md` contém a URL do deploy e a lista de variáveis de ambiente do projeto Vercel
    - `supabase/config.toml` contém a URL do deploy em additional_redirect_urls (ou pendência manual documentada no SUMMARY)
    - Nenhum secret commitado: `git log --diff-filter=A --name-only -1` não inclui arquivos .env
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana do esqueleto no ambiente deployado</name>
  <action>Apresentar ao usuário a URL do deploy e o roteiro de 7 passos abaixo; AGUARDAR a confirmação humana. Antes de pausar, garantir que os pré-requisitos do roteiro estão prontos: credenciais seed documentadas no SUMMARY e, se EMAIL_DRIVER=console, instruções de onde ver o link do convite nos logs da Vercel. PENDÊNCIA OBRIGATÓRIA no SUMMARY: enquanto EMAIL_DRIVER=console, registrar "ativar Resend (verificar domínio elitejuris.com.br no Resend e trocar EMAIL_DRIVER=resend)" como pendência aberta — o success criterion 2 (convite por e-mail real) só é considerado completo no beta após essa ativação.</action>
  <done>Usuário respondeu "aprovado" após completar os 7 passos, ou os problemas relatados foram corrigidos e re-verificados</done>
  <what-built>O walking skeleton da Fase 1 completo e no ar: monorepo + banco multi-tenant criptografado em São Paulo (sa-east-1) + login com papéis + convites + remoção + reset de senha + admin, deployado na Vercel. Testes cross-tenant verdes em CI (plano 01-03).</what-built>
  <how-to-verify>
    Na URL do deploy (registrada no README.md e no SUMMARY do plano):
    1. Entre com o gestor demo (e-mail/senha do seed — ver .env.local, SEED_GESTOR_EMAIL). Você deve ver "Escritório Demo" e a contagem de membros.
    2. Abra "Equipe" e convide um e-mail seu de teste com papel advogado. (Com EMAIL_DRIVER=console o link do convite aparece nos logs da Vercel — o executor colará o link para você no resumo; com Resend configurado, chega por e-mail.)
    3. Abra o link do convite em janela anônima, crie a conta (nome + senha) e confirme que ela aparece na lista da equipe.
    4. Ainda como gestor, remova esse advogado de teste e confirme que ele aparece com o selo "removido".
    5. Em janela anônima, tente logar com o advogado demo (SEED_ADVOGADO_EMAIL): deve aparecer o aviso de que o painel é exclusivo de gestores.
    6. Em /recuperar-senha, peça o link para o seu e-mail de gestor demo e complete a troca de senha.
    7. Entre com o super-admin (SEED_SUPERADMIN_EMAIL) e, em /admin, crie um escritório de teste com um e-mail de gestor.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se os 7 passos funcionaram, ou descreva o que falhou</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (este plano)

- Arquivos novos: `vercel.json`
- Arquivos modificados: `README.md` (seção Deploy), `supabase/config.toml` (redirect URLs de produção)
- Infra: projeto Vercel (região gru1) com env vars configuradas; URL pública do painel

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → URL pública Vercel | Painel exposto publicamente pela primeira vez |
| Vercel env → runtime | Secrets do Supabase vivem nos secrets do projeto Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-31 | Information Disclosure | service_role key vazando no bundle público | mitigate | Variável sem prefixo público, usada só em admin.ts (gate do 01-04); conferir no build da Vercel que ela não aparece em artefatos do cliente |
| T-1-32 | Tampering | Redirect de auth para domínio errado (link de reset no ar) | mitigate | additional_redirect_urls restrito a localhost + URL do deploy; validação de next interno já testada no 01-06 |
| T-1-33 | Information Disclosure | Processamento fora do Brasil (dúvida jurídica A6) | accept | Dados em repouso ficam no Supabase sa-east-1 (LGPD-01 como escrito); funções em gru1; interpretação registrada para confirmação do dono no verify-work |
| T-1-34 | Denial of Service | Deploy sem os testes da fase | mitigate | Deploy depende dos planos 01-05/06/07 concluídos; CI db-tests verde é pré-condição do gate de fase |
</threat_model>

<verification>
- /login responde 200 na URL pública; smoke test verde
- Checkpoint humano aprovado (7 passos do fluxo da fase no ambiente real)
</verification>

<success_criteria>
- Walking skeleton deployado: scaffold → routing → leitura/escrita real no banco → interação de UI → ambiente dev no ar
- Dono validou pessoalmente login, convite, remoção, reset e admin no ambiente real
- Fase 2 tem uma API pública para a extensão consumir
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-backend-multi-tenant/01-08-SUMMARY.md` when done
</output>
