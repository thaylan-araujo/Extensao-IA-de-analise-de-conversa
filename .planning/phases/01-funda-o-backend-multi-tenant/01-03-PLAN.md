---
phase: 01-funda-o-backend-multi-tenant
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - supabase/tests/00-helpers.sql
  - supabase/tests/01-cross-tenant.test.sql
  - supabase/tests/02-roles.test.sql
  - supabase/tests/03-removal.test.sql
  - supabase/tests/04-rls-coverage.test.sql
  - .github/workflows/db-tests.yml
autonomous: true
requirements: [AUTH-04, LGPD-01]
user_setup:
  - service: github
    why: "Os testes pgTAP rodam em GitHub Actions (Docker indisponível na máquina local — Pitfall 7 da pesquisa); o repositório precisa existir no GitHub com Actions habilitado"
    dashboard_config:
      - task: "Nenhuma ação manual esperada — o executor usa a gh CLI (já autenticada) para criar/apontar o repositório remoto se não existir"
        location: "github.com"
must_haves:
  truths:
    - "Testes automatizados com duas organizações provam que nenhuma consulta vaza dados entre organizações (criterion 4)"
    - "Advogado não lê conversas de colegas; gestor lê tudo da própria org; super_admin atravessa de forma controlada"
    - "Membro com status removed perde acesso na consulta seguinte, sem refresh de token (D-11)"
    - "Nenhuma tabela do schema public existe sem RLS habilitado"
    - "A suíte pgTAP roda verde em CI no GitHub Actions"
  artifacts:
    - path: "supabase/tests/01-cross-tenant.test.sql"
      provides: "Prova de isolamento entre org A e org B, incluindo escrita real em conversations/messages (LGPD-01: tabelas centrais aceitam escrita)"
    - path: "supabase/tests/04-rls-coverage.test.sql"
      provides: "Gate estrutural: falha se qualquer tabela public estiver sem rowsecurity"
    - path: ".github/workflows/db-tests.yml"
      provides: "CI: supabase start + supabase test db + vitest do apps/web"
  key_links:
    - from: ".github/workflows/db-tests.yml"
      to: "supabase/tests/"
      via: "supabase test db executa toda a suíte pgTAP contra as migrations"
      pattern: "supabase test db"
    - from: "supabase/tests/00-helpers.sql"
      to: "auth.users"
      via: "tests.create_supabase_user / tests.authenticate_as simulam usuários reais com JWT"
      pattern: "authenticate_as"
---

<objective>
Provar, com testes pgTAP executados em CI, o isolamento multi-tenant do schema criado em 01-02: duas organizações, papéis gestor/advogado/super_admin, remoção com efeito imediato e cobertura de RLS em 100% das tabelas public.

Purpose: criterion 4 da fase exige "testes cross-tenant automatizados (duas organizações) passam em CI". Este é também o portão que libera o Painel do Gestor na Phase 5 (decisão de roadmap). Docker está ausente na máquina local (Pitfall 7), então o gate autoritativo é o CI do GitHub Actions.
Output: suíte pgTAP versionada + workflow de CI verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md
@supabase/migrations/
@.planning/phases/01-funda-o-backend-multi-tenant/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suíte pgTAP — cross-tenant, papéis, remoção imediata e cobertura de RLS</name>
  <files>supabase/tests/00-helpers.sql, supabase/tests/01-cross-tenant.test.sql, supabase/tests/02-roles.test.sql, supabase/tests/03-removal.test.sql, supabase/tests/04-rls-coverage.test.sql</files>
  <read_first>
    - supabase/migrations/*_initial_schema.sql (estado real do schema: nomes de tabelas, colunas, helpers e policies criados em 01-02 — os testes DEVEM usar esses nomes)
    - .planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md (seção "Code Examples" — teste pgTAP de referência; Assumption A7 sobre os helpers basejump)
  </read_first>
  <action>
    Criar supabase/tests/00-helpers.sql com os helpers de teste: vendorizar o SQL do basejump supabase_test_helpers (github.com/usebasejump/supabase-test-helpers, ~0.0.6) fornecendo tests.create_supabase_user(identifier) e tests.authenticate_as(identifier). Assumption A7: se o SQL vendorizado for incompatível com a CLI 2.109.x, escrever helpers próprios mínimos (~50 linhas) que inserem em auth.users e trocam o contexto via set local role authenticated + set local request.jwt.claims com sub do usuário — comportamento equivalente é o que importa, não a origem do código.

    Escrever os quatro arquivos de teste (padrão pgTAP: begin; select plan(N); ...; select * from finish(); rollback;):

    01-cross-tenant.test.sql — setup como service_role: org A e org B em organizations, um gestor em cada (profiles role gestor), uma conversation + message em cada org (escrita real nas tabelas centrais — evidência LGPD-01 de que transcrições são graváveis no schema). Asserts autenticado como gestor A: select count(*) em organizations retorna 1; conversations retorna só a da org A; select em profiles/conversations/messages/invitations filtrando pela org B retorna vazio. Espelhar autenticado como gestor B contra a org A.

    02-roles.test.sql — na org A: gestor + dois advogados, cada advogado com uma conversation própria. Asserts: advogado 1 vê apenas a própria conversation (não a do advogado 2 — criterion 1 "advogado não vê dados dos colegas"); advogado 1 vê o próprio profile mas NÃO o profile do advogado 2; gestor vê as duas conversations e os profiles da equipe; advogado não lê invitations; super_admin (profile em org interna separada) lê conversations de qualquer org; advogado NÃO consegue insert em conversations com organization_id da org B nem com profile_id de outro usuário (with check nega); authenticated não consegue insert em diagnostics nem em audit_log.

    03-removal.test.sql — D-11 no nível do banco: criar advogado com conversation; autenticado como ele, count(conversations) = 1; como service_role, update profiles set status='removed'; autenticado NOVAMENTE como o mesmo usuário (mesmo JWT, sem refresh), select em conversations e organizations retorna 0 linhas — os helpers security definer leem status a cada query. Assert adicional: gestor da org continua vendo a conversation do removido (histórico preservado).

    04-rls-coverage.test.sql — gate estrutural: select count(*) from pg_tables where schemaname='public' and not rowsecurity deve ser 0; e para cada uma das 7 tabelas do domínio, existe ao menos 1 policy em pg_policies.
  </action>
  <verify>
    <automated>ls supabase/tests/00-helpers.sql supabase/tests/01-cross-tenant.test.sql supabase/tests/02-roles.test.sql supabase/tests/03-removal.test.sql supabase/tests/04-rls-coverage.test.sql && grep -l "authenticate_as" supabase/tests/0[1-3]*.sql | wc -l | grep -q 3 && grep -q "rowsecurity" supabase/tests/04-rls-coverage.test.sql && grep -q "plan(" supabase/tests/01-cross-tenant.test.sql</automated>
  </verify>
  <done>Cinco arquivos pgTAP escritos contra o schema real, cobrindo cross-tenant, papéis, remoção imediata e cobertura de RLS</done>
  <acceptance_criteria>
    - Os 5 arquivos existem em supabase/tests/
    - 01-cross-tenant.test.sql contém insert em conversations E em messages (escrita real) e asserts de vazio contra a org oposta
    - 02-roles.test.sql contém asserts para advogado (não vê colega), gestor (vê equipe) e super_admin (atravessa)
    - 03-removal.test.sql contém update de profiles para status removed seguido de assert de 0 linhas SEM novo create de usuário
    - 04-rls-coverage.test.sql consulta pg_tables com rowsecurity e pg_policies
    - Cada arquivo .test.sql contém select plan( e finish() com rollback
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CI GitHub Actions — pgTAP + vitest verdes no remoto</name>
  <files>.github/workflows/db-tests.yml</files>
  <read_first>
    - .planning/phases/01-funda-o-backend-multi-tenant/01-RESEARCH.md (seção "Code Examples" — workflow de referência; Pitfall 7)
    - supabase/tests/ (suíte da Task 1)
    - apps/web/vitest.config.ts (comando de teste do app)
  </read_first>
  <action>
    Criar .github/workflows/db-tests.yml com trigger on: [push, pull_request] e dois jobs:
    - job rls: ubuntu-latest; actions/checkout@v4; supabase/setup-cli@v1 (version pinada 2.109.1); supabase start; supabase test db. Os runners do GitHub têm Docker — é aqui que a suíte pgTAP roda de verdade (local não tem Docker, Pitfall 7).
    - job web-tests: ubuntu-latest; checkout; corepack enable pnpm; setup-node 22 com cache pnpm; pnpm install; pnpm --filter web exec vitest run --exclude tests/skeleton.test.ts (o teste de esqueleto exige o projeto hospedado e secrets — fica fora do CI nesta fase; testes unitários dos planos seguintes rodam aqui).

    Garantir remoto: verificar git remote -v; se não houver remoto, criar repositório privado via gh repo create (gh CLI instalada e autenticada) e apontar origin. Push do branch atual e acompanhar a execução com gh run watch até conclusão. Se o job rls falhar por incompatibilidade dos helpers basejump (A7), aplicar o fallback descrito na Task 1 e re-push.
  </action>
  <verify>
    <automated>gh run list --workflow=db-tests.yml --limit 1 --json conclusion -q '.[0].conclusion' | grep -q success</automated>
  </verify>
  <done>Workflow versionado e última execução do db-tests.yml concluída com success no GitHub Actions</done>
  <acceptance_criteria>
    - `.github/workflows/db-tests.yml` contém `supabase test db` e `vitest run`
    - `gh run list --workflow=db-tests.yml --limit 1 --json conclusion` retorna `success`
    - O job rls executa `supabase start` antes de `supabase test db`
    - A versão da CLI no workflow está pinada (2.109.1), não `latest` solto
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (este plano)

- Arquivos novos: `supabase/tests/00-helpers.sql`, `supabase/tests/01-cross-tenant.test.sql`, `supabase/tests/02-roles.test.sql`, `supabase/tests/03-removal.test.sql`, `supabase/tests/04-rls-coverage.test.sql`, `.github/workflows/db-tests.yml`
- Símbolos novos (SQL de teste): funções `tests.create_supabase_user`, `tests.authenticate_as` (vendorizadas/próprias)
- Infra: repositório GitHub com Actions rodando `db-tests` (jobs `rls` e `web-tests`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suíte de teste → Postgres local do CI | Testes simulam usuários autenticados de orgs distintas |
| Repositório → GitHub Actions | Workflow roda com o código do repo; sem secrets de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Information Disclosure | Vazamento cross-tenant não detectado (regressão futura de policy) | mitigate | Suíte pgTAP roda em todo push/PR; 04-rls-coverage falha se nova tabela nascer sem RLS |
| T-1-10 | Broken Access Control | Remoção sem efeito imediato passa despercebida | mitigate | 03-removal.test.sql assert de 0 linhas sem refresh de token (D-11) |
| T-1-11 | Information Disclosure | Secrets de produção no CI | mitigate | Jobs desta fase não usam nenhum secret do projeto hospedado; pgTAP roda contra stack efêmera do runner |
</threat_model>

<verification>
- `gh run list --workflow=db-tests.yml` mostra a última execução com conclusion success
- Suíte cobre: cross-tenant (2 orgs), papéis (3 papéis), remoção imediata (D-11), cobertura RLS (0 tabelas sem rowsecurity)
</verification>

<success_criteria>
- Criterion 4 da fase PROVADO: testes cross-tenant com duas organizações verdes em CI
- Escrita real em conversations/messages coberta (base da evidência LGPD-01)
- Gate estrutural impede tabela futura sem RLS
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-backend-multi-tenant/01-03-SUMMARY.md` when done
</output>
