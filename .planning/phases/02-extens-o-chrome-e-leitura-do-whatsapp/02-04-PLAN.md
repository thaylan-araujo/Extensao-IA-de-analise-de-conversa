---
phase: 02-extens-o-chrome-e-leitura-do-whatsapp
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/extension/entrypoints/whatsapp.content/reader/selectors.ts
  - apps/extension/entrypoints/whatsapp.content/reader/extract.ts
  - apps/extension/entrypoints/whatsapp.content/reader/canary.ts
  - apps/extension/tests/extract.test.ts
  - apps/extension/tests/canary.test.ts
  - apps/extension/tests/read-only-gate.test.ts
  - .github/workflows/extension-ci.yml
autonomous: true
requirements: [EXT-03, EXT-08]
must_haves:
  truths:
    - "O parser extrai texto, remetente, horário e direção (from_me) das fixtures REAIS capturadas no spike (EXT-03)"
    - "Mensagens de mídia viram marcadores pt-BR: [áudio], [imagem], [documento]; linha não parseável vira [não suportado], nunca descartada em silêncio"
    - "Linha com chatId de grupo é identificada como grupo (D-02) a partir do sufixo do data-id"
    - "O canário distingue leitura quebrada de WhatsApp desconectado (tela de QR não dispara falso positivo)"
    - "Nenhum módulo de reader/ ou sync/ usa APIs de escrita/eventos sintéticos/scroll no DOM — verificado por teste estático que roda localmente e no CI (EXT-08)"
  artifacts:
    - path: "apps/extension/entrypoints/whatsapp.content/reader/selectors.ts"
      provides: "Única fonte de seletores, cadeias com fallback ordenadas por estabilidade, baseadas no 02-SPIKE.md"
      contains: "data-id"
    - path: "apps/extension/entrypoints/whatsapp.content/reader/extract.ts"
      provides: "extractMessageRow (DOM → MessageDTO), parsePtBrTimestamp, detecção de mídia — funções puras"
      contains: "extractMessageRow"
    - path: "apps/extension/entrypoints/whatsapp.content/reader/canary.ts"
      provides: "evaluateCanary — heurísticas quebrado/drift/ok/desconectado"
      contains: "evaluateCanary"
    - path: "apps/extension/tests/read-only-gate.test.ts"
      provides: "Gate estático EXT-08: varre reader/ e sync/ proibindo APIs de escrita"
      min_lines: 30
    - path: ".github/workflows/extension-ci.yml"
      provides: "CI da extensão: vitest (inclui o gate) + build a cada push/PR"
  key_links:
    - from: "apps/extension/entrypoints/whatsapp.content/reader/extract.ts"
      to: "apps/extension/entrypoints/whatsapp.content/reader/selectors.ts"
      via: "todo seletor vem do módulo SEL — nenhum seletor inline"
      pattern: "from './selectors'"
    - from: "apps/extension/tests/extract.test.ts"
      to: "apps/extension/tests/fixtures"
      via: "testes carregam o HTML real sanitizado do spike"
      pattern: "fixtures"
    - from: "apps/extension/entrypoints/whatsapp.content/reader/extract.ts"
      to: "packages/shared/src/index.ts"
      via: "retorna MessageDTO do contrato compartilhado"
      pattern: "MessageDTO"
---

<objective>
Construir o núcleo genuinamente novo do produto — o parser de mensagens do WhatsApp (EXT-03) e o canário de quebra (base do EXT-05/D-13) — como funções puras testadas contra as fixtures reais do spike, e instalar a garantia arquitetural verificável de somente-leitura (EXT-08): gate estático que falha o build/CI se qualquer API de escrita no DOM aparecer em reader/ ou sync/.

Purpose: o RESEARCH identifica o reader como o único código sem padrão pronto e o ponto de manutenção contínua do produto — concentrar aqui o esforço de testes. O gate EXT-08 é a mitigação primária do risco de banimento (a Meta bane automação de ENVIO; leitura passiva não).
Output: parser + canário verdes contra fixtures reais, gate somente-leitura ativo localmente e no CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-CONTEXT.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-SPIKE.md
@packages/shared/src/index.ts
@.github/workflows/db-tests.yml
</context>

## Phase Goal

**Como** advogado atendendo leads no WhatsApp Web, **quero** um painel lateral da extensão com sessão persistente enquanto a conversa ativa é lida e sincronizada automaticamente com o servidor, **para que** meu atendimento fique registrado para a orientação da IA e a auditoria do gestor — sem que a extensão jamais escreva no chat.

*(História derivada do Goal e dos success criteria da Fase 2 no ROADMAP.md — mesmo tratamento da Fase 1.)*

## Artifacts this phase produces (este plano)

- Módulo `reader/selectors.ts`: export `SEL` (objeto com cadeias de seletores: messageRow, prePlainText, messageText, header, appRoot, mediaHints) e `resolveWithFallback()`
- Módulo `reader/extract.ts`: exports `extractMessageRow(row: Element): MessageDTO | null`, `extractVisibleMessages(main: Element): MessageDTO[]`, `parsePtBrTimestamp(date: string, time: string): string | null`, `detectMediaKind(row: Element): MessageKind`, `parseChatIdFromDataId(dataId: string)`, `isGroupChatId(chatId: string): boolean`
- Módulo `reader/canary.ts`: export `evaluateCanary(input): CanaryVerdict` com tipo `CanaryVerdict` = "ok" | "drift" | "broken" | "disconnected" | "no_chat"
- Testes: `tests/extract.test.ts`, `tests/canary.test.ts`, `tests/read-only-gate.test.ts`
- Workflow CI novo: `.github/workflows/extension-ci.yml`

<tasks>

<task type="auto">
  <name>Task 1: RED — testes do parser e do canário contra as fixtures reais (devem falhar)</name>
  <files>apps/extension/tests/extract.test.ts, apps/extension/tests/canary.test.ts</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-SPIKE.md (âncoras VALIDADAS — fonte de verdade acima do RESEARCH; heurísticas de mídia observadas)
    - apps/extension/tests/fixtures/ (HTML real sanitizado capturado no spike — listar os arquivos disponíveis)
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Code Examples — teste de parser contra fixture; Pattern 6 — tabela de sinais do canário)
    - packages/shared/src/index.ts (MessageDTO/MessageKind — shape do retorno)
  </read_first>
  <action>
    Escrever os testes ANTES de qualquer implementação e rodá-los confirmando FALHA (módulos reader/ ainda não existem). Commit vermelho com prefixo test(02-04).

    `tests/extract.test.ts` — carregar cada fixture de tests/fixtures/ no happy-dom e afirmar sobre o retorno de extractMessageRow: msg-in-text → fromMe false, kind text, content igual ao texto da fixture, sender e sentAt não nulos (formato ISO do parsePtBrTimestamp); msg-out-text → fromMe true; msg-audio → kind audio e content exatamente "[áudio]"; msg-image → kind image e "[imagem]"; msg-document → kind document e "[documento]"; msg-group → isGroupChatId(waChatId) true (D-02); linha sem os atributos esperados → kind other com content "[não suportado]" (nunca null silencioso quando há data-id); waMessageId sempre igual ao data-id completo; waChatId tratado como string opaca (aceitar tanto sufixo c.us quanto lid — Pitfall 9). Casos do parsePtBrTimestamp: "[14:32, 11/07/2026] Nome:" → ISO válido; formato inesperado → null sem lançar.

    `tests/canary.test.ts` — casos da tabela do Pattern 6 como entradas explícitas de evaluateCanary: appRoot presente + main presente + 0 linhas com conversa aberta por N ciclos → "broken"; mais de 30% das linhas sem parse → "broken"; fallback secundário em uso → "drift"; appRoot ausente (QR/loading) → "disconnected" (NUNCA broken — Pitfall 2); main ausente com appRoot presente → "no_chat"; tudo saudável → "ok".
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run tests/extract.test.ts tests/canary.test.ts; test $? -ne 0 && echo "RED confirmado"</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos de teste existem e importam de ../entrypoints/whatsapp.content/reader/
    - `vitest run tests/extract.test.ts tests/canary.test.ts` sai NÃO-zero (vermelho esperado — módulos ausentes)
    - Todos os marcadores pt-BR ([áudio], [imagem], [documento], [não suportado]) aparecem como expectativas literais nos testes
    - Commit test(02-04) realizado com os testes vermelhos
  </acceptance_criteria>
  <done>Rede de proteção contra drift de seletor escrita e vermelha — define o alvo exato da Task 2</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — selectors.ts, extract.ts e canary.ts a partir das âncoras validadas no spike</name>
  <files>apps/extension/entrypoints/whatsapp.content/reader/selectors.ts, apps/extension/entrypoints/whatsapp.content/reader/extract.ts, apps/extension/entrypoints/whatsapp.content/reader/canary.ts</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-SPIKE.md (seletores confirmados — usar os REAIS, não os assumidos do RESEARCH se divergirem)
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Pattern 4 — regras da cadeia de fallback; Code Examples — extractMessageRow de referência)
    - apps/extension/tests/extract.test.ts e tests/canary.test.ts (o contrato a satisfazer)
  </read_first>
  <action>
    Implementar os três módulos até os testes da Task 1 ficarem verdes, respeitando as regras do Pattern 4:

    `selectors.ts` é a ÚNICA fonte de seletores do app — nenhum seletor inline em extract/canary/observers. Cada alvo tem cadeia ordenada por estabilidade: atributo semântico (data-*) → role/aria → âncora estrutural relativa. PROIBIDO: classes ofuscadas geradas, seletores posicionais por índice de filho, XPath. Exportar também um mecanismo que registra quando um fallback secundário resolveu (sinal de drift consumido pelo canário e, no 02-06, reportado ao reader_status).

    `extract.ts`: funções puras sem efeito colateral (testáveis com fixture): parse do data-id no formato {fromMe}_{chatId}_{hash} com regex tolerante; direção pelo prefixo com fallback pelas classes de direção documentadas no spike; texto via âncora de texto do spike concatenando alt de emojis; timestamp/remetente do data-pre-plain-text com regex tolerante a locale; mídia por heurística de elementos conforme observado no spike (kind audio/image/document); sem âncora de texto nem heurística de mídia → kind other com marcador "[não suportado]". Try/catch no topo de extractVisibleMessages: exceção vira sinal para o canário, nunca crash do content script. wa_chat_id é string OPACA — nunca derivar telefone dela (Pitfall 9).

    `canary.ts`: evaluateCanary como função pura sobre um snapshot de sinais (appRoot presente?, main presente?, contagem de linhas, taxa de parse, fallback em uso?, exceção no ciclo?) devolvendo CanaryVerdict conforme a tabela do Pattern 6. A distinção disconnected vs broken é a mais importante (falso positivo do banner D-13 é o warning sign do Pitfall 2).

    Commit verde com prefixo feat(02-04).
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run tests/extract.test.ts tests/canary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run tests/extract.test.ts tests/canary.test.ts` sai 0 (verde)
    - extract.ts e canary.ts importam seletores exclusivamente de './selectors' (`grep -rn "querySelector" apps/extension/entrypoints/whatsapp.content/reader/extract.ts | grep -v "SEL\." | wc -l` = 0 ou equivalente: todo querySelector usa constantes de SEL)
    - extract.ts contém "[não suportado]" (transparência da transcrição)
    - Nenhuma função de extract/canary tem efeito colateral de rede ou DOM (só leitura de Element recebido por parâmetro)
  </acceptance_criteria>
  <done>Parser e canário verdes contra HTML real do WhatsApp — EXT-03 provado por teste de regressão</done>
</task>

<task type="auto">
  <name>Task 3: Gate somente-leitura (EXT-08) + CI da extensão</name>
  <files>apps/extension/tests/read-only-gate.test.ts, .github/workflows/extension-ci.yml</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Pattern 8 — lista de APIs proibidas e as duas exceções documentadas)
    - .github/workflows/db-tests.yml (convenções de CI do repo: setup de node/pnpm, triggers)
  </read_first>
  <action>
    Criar `tests/read-only-gate.test.ts`: teste estático que lê recursivamente os fontes .ts/.tsx de entrypoints/whatsapp.content/reader/ e entrypoints/whatsapp.content/sync/ (excluindo arquivos de teste) e FALHA se encontrar qualquer padrão da lista proibida do Pattern 8 — eventos sintéticos (dispatch de eventos), execução de comandos de edição do documento, chamadas de clique/foco programático, atribuições de HTML/valor em elementos, inserção de nós (append/prepend/insertAdjacent/removeChild), APIs de rolagem programática (D-03: a extensão nunca rola sozinha) e chrome.debugger. Cada padrão proibido vive como regex numa lista nomeada no próprio teste, com mensagem de falha apontando arquivo+linha. Decisão de discretion documentada: gate como teste vitest + step de CI em vez de plugin ESLint (o repo não tem ESLint configurado; mesma garantia, zero dependência nova — o RESEARCH sugeria ESLint OU grep no CI).

    O escopo do gate é reader/ e sync/ por design: as duas únicas mutações permitidas na página (host do painel e reserva de largura) vivem em index.tsx/panel, FORA do escopo — lista fechada de exceções do Pattern 8, já documentada no 02-03.

    Criar `.github/workflows/extension-ci.yml`: dispara em push/PR que toque apps/extension/** ou packages/shared/**; jobs: install (mesmo setup node/pnpm do db-tests.yml), `pnpm --filter extension exec vitest run` (inclui o gate) e `pnpm --filter extension build`. O gate rodando no CI transforma EXT-08 em garantia arquitetural verificável: o build quebra se alguém introduzir escrita no DOM.
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run tests/read-only-gate.test.ts && node -e "const s=require('fs').readFileSync('.github/workflows/extension-ci.yml','utf8');['apps/extension','vitest','build'].forEach(t=>{if(!s.includes(t))throw new Error('ci sem: '+t)});console.log('ci ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run tests/read-only-gate.test.ts` sai 0 com o código atual de reader/ e sync/
    - Sabotagem manual comprovada: adicionar temporariamente uma chamada proibida em reader/extract.ts faz o gate FALHAR; reverter em seguida (provar que o gate morde — registrar no SUMMARY)
    - extension-ci.yml existe com trigger em apps/extension/** e steps de test + build
    - Workflow verde no GitHub Actions após o push (`gh run list --workflow extension-ci.yml --limit 1`)
  </acceptance_criteria>
  <done>EXT-08 é garantia arquitetural verificável: gate ativo local + CI, com exceções em lista fechada documentada</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM do WhatsApp → reader | Conteúdo de mensagem é input hostil (XSS, formatos inesperados) |
| reader → resto da extensão | MessageDTO é o único formato que sai do reader |
| Código da extensão → página | Proibição total de escrita fora das duas exceções |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering / XSS | extract.ts (conteúdo de mensagem malicioso) | mitigate | Extração exclusivamente via textContent/getAttribute — HTML nunca entra no DTO; React escapa na renderização (ASVS V5) |
| T-02-13 | Tampering / risco de ban | extensão virar vetor de escrita acidental | mitigate | Gate estático read-only-gate.test.ts em reader/ e sync/ + CI (EXT-08); rolagem programática incluída na lista (D-03) |
| T-02-14 | Denial of Service | exceção no parser derruba o content script | mitigate | try/catch no topo do ciclo de extração → vira verdict broken do canário (fail-safe do Pattern 6) |
| T-02-15 | Information Disclosure | fixtures com dados reais no git | mitigate | Fixtures sanitizadas no 02-01 (nomes/telefones placeholder); revisão na Task 1 antes de usar |
| T-02-SC | Tampering | npm installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- Suíte da extensão inteira verde: `pnpm --filter extension exec vitest run`
- extension-ci.yml verde no GitHub Actions
- Prova de sabotagem do gate registrada no SUMMARY
</verification>

<success_criteria>
- Parser extrai texto/remetente/horário e marca mídias como [áudio]/[imagem]/[documento] a partir de HTML real (EXT-03)
- Grupo identificado pelo chatId para o D-02
- Canário distingue quebrado/drift/desconectado sem falso positivo de QR (base do EXT-05)
- Somente-leitura garantido por gate automatizado com exceções em lista fechada (EXT-08)
</success_criteria>

<output>
Create `.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-04-SUMMARY.md` when done
</output>
