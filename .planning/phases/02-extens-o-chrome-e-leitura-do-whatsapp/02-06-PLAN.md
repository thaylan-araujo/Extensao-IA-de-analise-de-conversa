---
phase: 02-extens-o-chrome-e-leitura-do-whatsapp
plan: 06
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/extension/entrypoints/whatsapp.content/reader/state.ts
  - apps/extension/entrypoints/whatsapp.content/reader/observers.ts
  - apps/extension/entrypoints/whatsapp.content/sync/schema.ts
  - apps/extension/entrypoints/whatsapp.content/sync/queue.ts
  - apps/extension/entrypoints/whatsapp.content/sync/flags.ts
  - apps/extension/entrypoints/whatsapp.content/index.tsx
  - apps/extension/entrypoints/whatsapp.content/panel/store.ts
  - apps/extension/tests/state.test.ts
  - apps/extension/tests/queue.test.ts
  - apps/extension/tests/flags.test.ts
autonomous: true
requirements: [EXT-02, EXT-04, EXT-05, EXT-07]
must_haves:
  truths:
    - "Ao abrir uma conversa individual, as mensagens carregadas na tela são extraídas e upsertadas no servidor; trocar de conversa re-aponta a leitura e o painel reflete o novo lead (EXT-02, D-01, D-03)"
    - "Reprocessar a mesma tela N vezes não duplica mensagens — upsert com ignoreDuplicates sobre a unique constraint (EXT-04)"
    - "Grupo aberto → nada é sincronizado e o painel mostra o aviso de grupo (D-02)"
    - "Canário em broken OU flag reader_enabled false → observers desconectados, sync parado, banner de manutenção no painel, status reportado em reader_status; recuperação automática sem ação do advogado (EXT-05, D-13, D-15)"
    - "Recolher o painel NÃO pausa a leitura; logout/remoção param imediatamente (D-04, D-11)"
    - "Nenhum polling de DOM: observers estreitos com debounce e extração em requestIdleCallback (EXT-07)"
  artifacts:
    - path: "apps/extension/entrypoints/whatsapp.content/reader/observers.ts"
      provides: "Observer de troca de conversa + observer de mensagens (debounce 500ms, extração em idle)"
      contains: "requestIdleCallback"
    - path: "apps/extension/entrypoints/whatsapp.content/reader/state.ts"
      provides: "Reducer puro do estado de leitura (resolveView) — testável sem DOM"
      contains: "resolveView"
    - path: "apps/extension/entrypoints/whatsapp.content/sync/queue.ts"
      provides: "Fila com dedup local, flush em lote idempotente, backoff e erros de primeira classe"
      contains: "ignoreDuplicates"
    - path: "apps/extension/entrypoints/whatsapp.content/sync/flags.ts"
      provides: "healthCycle: poll do kill-switch + heartbeat reader_status + re-teste automático de quebra"
      contains: "reader_enabled"
  key_links:
    - from: "apps/extension/entrypoints/whatsapp.content/reader/observers.ts"
      to: "apps/extension/entrypoints/whatsapp.content/reader/extract.ts"
      via: "callback debounced chama extractVisibleMessages"
      pattern: "extractVisibleMessages"
    - from: "apps/extension/entrypoints/whatsapp.content/sync/queue.ts"
      to: "apps/extension/entrypoints/whatsapp.content/sync/supabase.ts"
      via: "upsert de conversations e messages sob a sessão do advogado (RLS)"
      pattern: "upsert"
    - from: "apps/extension/entrypoints/whatsapp.content/sync/flags.ts"
      to: "apps/extension/entrypoints/whatsapp.content/panel/store.ts"
      via: "kill-switch/broken alimentam resolveView → banner D-13"
      pattern: "kill_switch"
---

<objective>
Fechar a fatia vertical central da fase: conversa individual aberta no WhatsApp → observers estreitos detectam (EXT-02) → parser extrai (02-04) → fila sincroniza em lotes idempotentes sob a sessão do advogado (EXT-04, D-01) → painel mostra lead + "Conversa monitorada" — com kill-switch remoto, heartbeat de saúde, detecção de quebra com recuperação automática (EXT-05, D-13, D-15) e sem degradar a performance do WhatsApp (EXT-07).

Purpose: é o momento em que o produto passa a existir de ponta a ponta: o que o advogado conversa aparece no banco para as fases 3-5 consumirem.
Output: pipeline leitura→sync completo ligado ao painel, com testes de reducer, fila e flags.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-CONTEXT.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md
@.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-SPIKE.md
@apps/extension/entrypoints/whatsapp.content/reader/extract.ts
@apps/extension/entrypoints/whatsapp.content/panel/store.ts
@apps/extension/entrypoints/whatsapp.content/sync/supabase.ts
@packages/shared/src/index.ts
</context>

## Phase Goal

**Como** advogado atendendo leads no WhatsApp Web, **quero** um painel lateral da extensão com sessão persistente enquanto a conversa ativa é lida e sincronizada automaticamente com o servidor, **para que** meu atendimento fique registrado para a orientação da IA e a auditoria do gestor — sem que a extensão jamais escreva no chat.

*(História derivada do Goal e dos success criteria da Fase 2 no ROADMAP.md — mesmo tratamento da Fase 1.)*

## Artifacts this phase produces (este plano)

- Módulo `reader/state.ts`: export `resolveView(signals): PanelView` e tipo `ReaderSignals` (session, profileRemoved, waConnected, activeChat: { chatId, isGroup, contactName } | null, canary: CanaryVerdict, killSwitch: boolean)
- Módulo `reader/observers.ts`: exports `startConversationObserver()`, `startMessageObserver()`, `stopAllObservers()`
- Módulo `sync/schema.ts`: schema Zod `messageDtoSchema` validando MessageDTO no boundary DOM→sync (decisão do 02-02: Zod vive na extensão, não em shared)
- Módulo `sync/queue.ts`: export `createSyncQueue()` com `enqueue()`, `flush()`, `onError()`
- Módulo `sync/flags.ts`: exports `startHealthCycle()`, `stopHealthCycle()`
- Testes: `tests/state.test.ts`, `tests/queue.test.ts`, `tests/flags.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Máquina de estados da leitura + observers estreitos com debounce (EXT-02, EXT-07)</name>
  <files>apps/extension/entrypoints/whatsapp.content/reader/state.ts, apps/extension/entrypoints/whatsapp.content/reader/observers.ts, apps/extension/tests/state.test.ts</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Pattern 3 — dois observers, debounce, regras EXT-07; Pitfall 2 e 5)
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-SPIKE.md (âncora real do container do main e do header)
    - apps/extension/entrypoints/whatsapp.content/reader/extract.ts e canary.ts (contratos extractVisibleMessages/evaluateCanary do 02-04)
    - apps/extension/entrypoints/whatsapp.content/panel/store.ts (PanelView e insumos que o store espera)
  </read_first>
  <behavior>
    - resolveView: sem sessão → deslogado; profileRemoved → removido (prioridade sobre tudo exceto deslogado); killSwitch true → kill_switch; canary broken → quebrado; waConnected false → wa_desconectado (nunca quebrado — Pitfall 2); activeChat null → sem_conversa; activeChat isGroup → grupo; caso feliz → conversa_ativa
    - resolveView é indiferente ao estado collapsed (D-04: recolher não pausa nada)
    - drift NÃO muda a view (segue conversa_ativa) mas é reportado via flags (Task 3)
  </behavior>
  <action>
    `reader/state.ts`: implementar resolveView como função pura sobre ReaderSignals com a tabela de prioridade do bloco behavior. Escrever `tests/state.test.ts` ANTES (RED) cobrindo a matriz de transições acima e o caso D-04 (collapsed não aparece nem influencia), depois implementar (GREEN).

    `reader/observers.ts` (Pattern 3, com as âncoras reais do spike): (1) observer de conversa no container pai onde o painel principal da conversa monta/troca — childList raso, sem subtree profundo; ao disparar, resolver o chat ativo (chatId via data-id das linhas ou âncora do spike; detectar grupo via isGroupChatId — D-02; extrair nome/telefone exibível do header — A8, com fallback para o chatId) e recriar o observer de mensagens; (2) observer de mensagens APENAS na lista de mensagens do chat ativo (childList + subtree), callback com debounce de 500ms que agenda a extração em requestIdleCallback com timeout 2000ms — a extração NUNCA roda no handler síncrono da mutação (Pitfall 5). A cada ciclo de extração: rodar evaluateCanary com o snapshot e entregar os DTOs à fila (injetada por parâmetro — sem import circular). Regras EXT-07 hard: nenhum setInterval/setTimeout de polling do DOM; observers desconectados em logout/removido/kill-switch/quebrado ou sem conversa — mas NÃO quando o painel é recolhido (D-04). Rolagem manual do advogado gera mutações e as mensagens antigas entram naturalmente — a extensão jamais rola por conta própria (D-03; o gate do 02-04 já proíbe as APIs de rolagem).
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run tests/state.test.ts && pnpm --filter extension exec vitest run tests/read-only-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run tests/state.test.ts` sai 0 com a matriz completa de resolveView
    - observers.ts contém debounce de 500ms e requestIdleCallback (extração fora do caminho crítico)
    - observers.ts passa no gate somente-leitura do 02-04 (roda na verify)
    - Nenhuma chamada de observe em document.body com subtree true (anti-padrão nº 1 do RESEARCH — alvo sempre estreito)
  </acceptance_criteria>
  <done>Troca de conversa detectada com observers estreitos; estado do painel correto para todos os cenários; performance por construção</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fila de sincronização idempotente sob RLS (EXT-04, D-01)</name>
  <files>apps/extension/entrypoints/whatsapp.content/sync/schema.ts, apps/extension/entrypoints/whatsapp.content/sync/queue.ts, apps/extension/tests/queue.test.ts</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Pattern 5 — fluxo conversa→mensagens; Code Examples flush; Pitfall 3 e 6; Don't Hand-Roll — fila simples, não framework)
    - apps/extension/entrypoints/whatsapp.content/sync/supabase.ts (cliente único tipado do 02-03)
    - packages/shared/src/index.ts (MessageDTO/MessageKind)
    - supabase/migrations/20260712000000_extension_sync.sql (nomes exatos de colunas e índices de conflito)
  </read_first>
  <behavior>
    - enqueue de DTOs duplicados (mesmo waMessageId) na mesma sessão → um único item pendente (Set local, otimização de rede)
    - flush resolve a conversa: upsert em conversations com onConflict profile_id,wa_chat_id (atualizando contact_name e updated_at) e recupera o id
    - flush envia messages em lote com onConflict conversation_id,wa_message_id e ignoreDuplicates true; linhas incluem organization_id, from_me, kind, sender (fallback: advogado quando fromMe, senão lead), content, sent_at
    - flush automático: ~3s após o primeiro enqueue pendente OU ao atingir 20 itens, o que vier primeiro
    - erro de rede → backoff exponencial (1s, 2s, 4s... teto 60s) e callback onError notificado — NUNCA erro engolido (Pitfall 3); os dados seguem no DOM (re-extração recupera, perda de lote não é perda de dado)
    - erro 401/403 → onError com marcação de auth para o chamador reavaliar a sessão (D-11)
    - DTO que falhe no messageDtoSchema (Zod) é descartado com aviso — nunca enviado malformado
  </behavior>
  <action>
    Criar `sync/schema.ts` com `messageDtoSchema` (Zod 4) espelhando MessageDTO — validação do boundary DOM→sync (ASVS V5; conteúdo do WhatsApp é input não confiável).

    Escrever `tests/queue.test.ts` ANTES (RED) com cliente supabase mockado (interceptar upsert e simular sucesso/erro/401) e fake timers do vitest para debounce/backoff, cobrindo os comportamentos do bloco behavior. Implementar `sync/queue.ts` (GREEN): createSyncQueue recebe { client, getContext } (contexto: user id, organization_id do profile, chat ativo) e mantém fila em memória + Set de waMessageId enviados. A idempotência REAL é a unique constraint do banco (02-02) — o Set é só economia de rede; recarregar a aba zera o Set sem risco de duplicar (Pitfall 6 e anti-padrão "dedup só no cliente"). Nunca logar conteúdo de mensagem em console/erros (LGPD — só contagens e ids truncados).
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run tests/queue.test.ts && pnpm --filter extension exec vitest run tests/read-only-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run tests/queue.test.ts` sai 0 com os 7 comportamentos do bloco behavior
    - queue.ts contém onConflict com conversation_id,wa_message_id e ignoreDuplicates (alvo exato da unique constraint)
    - queue.ts contém onConflict com profile_id,wa_chat_id para conversations
    - Nenhum console.log com content de mensagem em queue.ts (logs só metadados)
  </acceptance_criteria>
  <done>Transcrição sincronizada em lotes idempotentes sob a sessão do advogado — reprocessar nunca duplica (EXT-04)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Kill-switch + heartbeat + recuperação automática, e wiring final no content script (EXT-05, D-13, D-15)</name>
  <files>apps/extension/entrypoints/whatsapp.content/sync/flags.ts, apps/extension/entrypoints/whatsapp.content/index.tsx, apps/extension/entrypoints/whatsapp.content/panel/store.ts, apps/extension/tests/flags.test.ts</files>
  <read_first>
    - .planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-RESEARCH.md (Pattern 6 — ações ao entrar em QUEBRADO; Pattern 7 — healthCycle de referência; Pitfall 3)
    - apps/extension/entrypoints/whatsapp.content/index.tsx (wiring atual do 02-03)
    - apps/extension/entrypoints/whatsapp.content/panel/store.ts (insumos do resolveView)
  </read_first>
  <behavior>
    - healthCycle lê app_settings.reader_enabled: false → sinal killSwitch true (view kill_switch, mesmo visual do banner D-13; painel e login seguem vivos)
    - healthCycle upserta reader_status com status atual do canário (ok/drift/broken), extension_version do manifest e last_seen_at — uma escrita por ciclo (~5 min), piggyback no poll da flag
    - canário broken → observers/sync param, banner D-13 no painel, reader_status broken; a cada ciclo o self-check re-executa e, se a extração voltar a funcionar, a leitura retoma SOZINHA (D-13: "voltaremos automaticamente")
    - flag volta a true → leitura retoma sem ação do advogado (D-15)
    - falha de auth no ciclo (401/refresh recusado) → checkProfileStatus; removed → view removido e leitura para (D-11)
  </behavior>
  <action>
    Escrever `tests/flags.test.ts` ANTES (RED) com cliente mockado + fake timers cobrindo o bloco behavior. Implementar `sync/flags.ts` (GREEN): startHealthCycle({ client, getCanaryStatus, onSignals }) roda imediatamente no start e depois a cada 5 minutos (este setInterval é de REDE, não de DOM — permitido; a regra EXT-07 proíbe polling de DOM); o poll adicional acontece antes de cada flush da fila (hook exposto para a queue). Status vem de currentCanaryStatus; versão via browser.runtime.getManifest().version. details do reader_status leva SÓ metadados (verdict, seletor da cadeia em uso, contagem do último ciclo) — nunca transcrição.

    Wiring final em `index.tsx`: compor tudo — boot restaura sessão (02-03) → startHealthCycle → se leitura habilitada, startConversationObserver alimentando a queue → sinais (canário, kill-switch, auth, chat ativo, lead) fluem para o store e o resolveView decide a view. Logout e removido chamam stopAllObservers + stopHealthCycle + flush final cancelado. Recolher painel NÃO toca no reader (D-04). Sincroniza TODA conversa individual aberta — sem opt-in, sem lista de exceções (D-01; a válvula por conversa é ideia deferida, não construir).
  </action>
  <verify>
    <automated>pnpm --filter extension exec vitest run && pnpm --filter extension build</automated>
  </verify>
  <acceptance_criteria>
    - Suíte completa da extensão verde (state, queue, flags, extract, canary, auth-storage, gate) e build 0
    - flags.ts contém reader_enabled e reader_status (poll + heartbeat)
    - index.tsx liga observers→queue→flags→store (imports dos quatro módulos presentes)
    - flags.ts não contém conteúdo de mensagem em nenhum payload de details (inspeção: details recebe só verdict/contadores/versão)
  </acceptance_criteria>
  <done>Ciclo operacional completo: leitura remota desligável, saúde reportada, quebra detectada com aviso honesto e recuperação automática</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM WhatsApp → queue → Postgres | Conteúdo hostil atravessa para o banco multi-tenant |
| Extensão ↔ app_settings/reader_status | Extensão lê flag global e escreve só a própria linha de saúde |
| Sessão do advogado → RLS | Toda escrita herda as policies da org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Broken Access Control | advogado removido sincronizando | mitigate | RLS por lookup nega no servidor (Fase 1) + detecção 401→removido no ciclo (D-11) — defesa em profundidade provada em teste de flags |
| T-02-21 | Information Disclosure | logs/details com transcrição | mitigate | Contrato: nunca logar content; details só metadados — criterion explícito nas Tasks 2 e 3 |
| T-02-22 | Tampering | DTO malformado/malicioso no sync | mitigate | messageDtoSchema (Zod) no boundary + constraints/RLS como última linha (ASVS V5/V13) |
| T-02-23 | Denial of Service | flood de mutações degrada o WhatsApp | mitigate | Observers estreitos + debounce 500ms + requestIdleCallback + lotes de rede (EXT-07); profiling humano no 02-07 |
| T-02-24 | Spoofing | anon key usada fora da extensão | accept | Anon key é pública por design; RLS + auth obrigatória protegem (padrão da Fase 1) |
| T-02-SC | Tampering | npm installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- Suíte completa da extensão verde + build 0
- Gate somente-leitura continua verde com observers/queue/flags no escopo
- Matriz resolveView cobre os 9 estados incluindo D-04
</verification>

<success_criteria>
- Conversa individual aberta → transcrição no banco sem duplicatas, reagindo à troca de conversa (EXT-02, EXT-04, D-01)
- Grupo ignorado com aviso (D-02); histórico só do que o WhatsApp carregou, sem rolagem própria (D-03)
- Kill-switch remoto, heartbeat de saúde e quebra com recuperação automática funcionando de ponta a ponta (EXT-05, D-13, D-14, D-15)
- Zero polling de DOM; extração fora do caminho crítico (EXT-07)
</success_criteria>

<output>
Create `.planning/phases/02-extens-o-chrome-e-leitura-do-whatsapp/02-06-SUMMARY.md` when done
</output>
